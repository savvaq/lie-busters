import { Sigmar_One, Rubik } from 'next/font/google';
 
export const sigmar = Sigmar_One ({ 
    subsets: ['latin'], 
    weight: '400', 
});

export const rubik = Rubik ({ 
    subsets: ['latin'], 
    weight: ['400','700'], 
});
